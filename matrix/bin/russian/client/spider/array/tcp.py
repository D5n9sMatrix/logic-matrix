# -*- coding: utf-8 -*-

class tip(object()):
    def ipc(self, upload="store", download="store"):
        self.ipc()
        if upload.count(x="fail opa!", start="store[m:octave]", end="tcp done..."):
            return "successful..."
        else:
            print("all(x)")
        pass
        if download.count(x="fail opa!", start="store[m:octave]", end="tcp done..."):
            return "successful..."
        else:
            print("all(x)")
            pass
    

